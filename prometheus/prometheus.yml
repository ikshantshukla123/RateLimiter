global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: rate-limiter-gateway
    static_configs:
      - targets: ['gateway:3000']
    metrics_path: /metrics
    scrape_interval: 5s
